[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cocapn-training"
version = "0.1.0"
description = "Cocapn fleet training utilities"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = []

[project.urls]
Repository = "https://github.com/SuperInstance/cocapn-training"

[tool.hatch.build.targets.wheel]
packages = ["src/training"]
